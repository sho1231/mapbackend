open cmd in your project folder
first login using heroku login

then git config --global user.email "*emailid*"
then git config --global user.name "*name*"

git init (do it in project folder)
heroku git:remote -a *appname*

* if want to ignore certain files the specify those filesnames in .gitignore file*
git add .                   (. refers to the files and folders of current directory)

git commit -m "*Enter your message*"

git push heroku master

you are done