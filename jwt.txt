const jwt=require('jwt')
//to generate a auth token
const auth_token=jwt.sign(*object or string(not sure)*,*secretkey*,{expiresIn:1h})
const auth_token:jwt.sign({_id:user._id},"abc123",{
	expiresIn:1h
})



//to verify

const [tokentype,token]=req.headers.authorizarion.split(" ");
//the above code is done to get token type and token
 if(tokentype==="Bearer"){
	let{_id}=jwt.verify(token,*secretkey*);
	req._id=_id
//now will pass this below id for every event