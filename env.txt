will accept value like:
URL:WWW.GOOGLE.COM


//to access it

const {config}=require('dotenv');

//just needed in the index file..no need to call it in every folder as every folder is imported to index
config();

process.*VAR NAME*

note:boolean value in .env file is string so to use boolean value just compare like "==="false/true""